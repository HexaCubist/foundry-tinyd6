<form class="{{cssClass}}" autocomplete="off">
    {{> "systems/tinyd6/templates/partials/item-header.hbs" placeholder='tinyd6.armor.name'}}
    <table>
        <tr>
            <th class="text-nowrap">{{localize 'tinyd6.armor.type'}}</th>
            <th class="text-nowrap">{{localize 'tinyd6.armor.damageReduction'}}</th>
            <th class="text-nowrap">{{localize 'tinyd6.armor.slots'}}</th>
        </tr>
        <tr>
            <td>
                <select name="data.group">
                    {{#select system.group}}
                        {{#each config.armorTypes as |name type|}}
                            <option value="{{type}}">{{localize name}}</option>
                        {{/each}}
                    {{/select}}
                </select>
            </td>
            <td>
                <input type="text" class="text-center" name="data.damageReduction" value="{{system.damageReduction}}" data-dtype="Number"/>
            </td>
            <td>
                <input type="text" class="text-center" name="data.slots" value="{{system.slots}}" data-dtype="Number"/>
            </td>
        </tr>
    </table>
    {{editor content=system.description target="data.description" button=true owner=owner editable=editable}}
</form>
